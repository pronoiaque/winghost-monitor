# ─── Dépendances du BINAIRE Windows (PyInstaller) ────────────────────────────
# Contrairement au build « léger » v6.x, OpenCV est EMBARQUÉ : l'ancrage visuel
# (matchTemplate) est au cœur de la Couche 2, il n'est plus optionnel.
# Version headless d'OpenCV : pas de dépendances GUI Qt inutiles dans l'exe.

# Couches 1 & 2 — capture, ancrage visuel, injection
numpy>=1.24.0
opencv-python-headless>=4.8.0
mss>=9.0.0
pynput>=1.7.6
pyautogui>=0.9.54
Pillow>=10.0.0
screeninfo>=0.8.1

# Scheduler
APScheduler>=3.10.0

# Interface graphique — VERSION ÉPINGLÉE.
# L'IHM utilise l'API « classique » de Flet (ft.border.all, page.dialog,
# ft.Icons…), disponible jusqu'à la série 0.28.x. La réécriture « flet.controls.*
# » (série 0.80+) casse ces API (ex. « module 'flet.controls.border' has no
# attribute 'all' »). On épingle donc 0.28.3 — DERNIÈRE version de l'ère
# classique publiée À LA FOIS pour flet ET flet-desktop (flet-desktop n'existe
# pas en 0.24.x : sa plus ancienne release est 0.25.0).
flet==0.28.3
# Client desktop Flet : package SÉPARÉ. Indispensable au binaire (ft.app ouvre
# une fenêtre desktop) ; sinon Flet tente un pip install à l'exécution,
# impossible dans un exe figé. DOIT matcher la version de flet.
flet-desktop==0.28.3

# Windows
pywin32>=306

# Empaquetage
pyinstaller>=6.3.0
