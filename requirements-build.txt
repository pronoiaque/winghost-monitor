# ─── Dépendances du BINAIRE Windows (PyInstaller) ────────────────────────────
# Contrairement au build « léger » v6.x, OpenCV est EMBARQUÉ : l'ancrage visuel
# (matchTemplate) est au cœur de la Couche 2, il n'est plus optionnel.
# Version headless d'OpenCV : pas de dépendances GUI Qt inutiles dans l'exe.

# Couches 1 & 2 — capture, ancrage visuel, injection
numpy>=1.24.0
opencv-python-headless>=4.8.0
mss>=9.0.0
pynput>=1.7.6
pyautogui>=0.9.54
Pillow>=10.0.0
screeninfo>=0.8.1

# Scheduler
APScheduler>=3.10.0

# Interface graphique — VERSION ÉPINGLÉE.
# L'IHM est écrite pour l'API Flet 0.24 (ft.border.all, page.dialog, ft.icons…).
# Les Flet récents (package « flet.controls.* » réécrit) cassent ces API
# (ex. « module 'flet.controls.border' has no attribute 'all' »). On épingle
# donc une version 0.24.x stable, et le client desktop à la MÊME version.
flet==0.24.1
# Client desktop Flet : package SÉPARÉ depuis Flet ≥ 0.21. Indispensable au
# binaire (ft.app ouvre une fenêtre desktop) ; sinon Flet tente un pip install
# à l'exécution, impossible dans un exe figé. DOIT matcher la version de flet.
flet-desktop==0.24.1

# Windows
pywin32>=306

# Empaquetage
pyinstaller>=6.3.0
